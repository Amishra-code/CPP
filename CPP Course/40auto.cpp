#include<iostream>
using namespace std;

int main()
{
    auto age = 10;
    
    // auto i;    {give an error bcz we have not initialized the value so it can't fetch the datat type all by itself}
    
    cout<< age;

    return 0;

}